---
const { titulo, lenguaje, autor, descripcion, duracion, id, langs, expectedLang } = Astro.props;
---

<article>
    <img src={`/assets/images/langs/${lenguaje}.png`} alt="Lenguaje no reconocido">
    <div>
        <h2>{titulo}</h2>
        <h3 class="author">{autor}</h3>
        <span>{duracion} minutos</span>
        <p>Descripción:<br/> {descripcion}</p>
    </div>
    <a href={`/${expectedLang in langs ? expectedLang : langs[0]}/blog/${id}`}></a>
</article>