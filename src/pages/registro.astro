---
import Template from "../template/Template.astro";
import Form, { type FormProps } from "../components/react/Form.tsx";
import Breadcrumb from "../components/Breadcrumb.astro";

const formConfig: FormProps = {
    inputs: [
        {
            id: 'email', label: 'Correo', type: 'email', validator: 'email'
        },
        {
            id: 'name', label: 'Nombre', type: 'text', validator: 'text'
        },
        {
            id: 'last_name', label: 'Apellido', type: 'text', validator: 'text'
        },
        {
            id: 'password', label: 'Contraseña', type: 'password', validator: 'password'
        },
        {
            id: 'confirm', label: 'Contraseña', type: 'password', validator: 'password'
        }
    ],
    refs: [
        { text: 'Iniciar sesión', href: '/inicio-sesion' }
    ],
    title: 'Registro',
    uri: '',
    method: 'POST',
    redirect: '/blog'
}
const routes = [
  { text: 'Brayan Téllez', href: '/' },
  { text: 'Registro', href: '/registro' }
];
---
<Template includes={['form']}>
    <Breadcrumb routes={routes}/>
    <section id="main">
        <Form {...formConfig} client:only="react"/>
    </section>
</Template>