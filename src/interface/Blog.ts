export interface IBlogDefinition {
    id: string
    tags: string[]
};

export interface IBlogResume {
    id: string
    lang: string
    title: string
    description: string
    duration: number
    technology: string
    author: string
};